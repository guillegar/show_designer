# Virtual environments
venv/
venv311/
env/
ENV/
.venv

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Testing
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.hypothesis/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# OS
Thumbs.db
.DS_Store

# Logs
*.log
logs/

# Local env files
.env
.env.local
*.env

# Node (for web/ folder if it has npm)
node_modules/
npm-debug.log
yarn-error.log
dist/

# Build artifacts
*.o
*.a

# Audio / media — regenerable o externo, no versionar (ANALYSIS hallazgo 22)
*.mp3
*.wav
*.flac
*.ogg

# Análisis cacheado — regenerable con analyzer_pro / demucs (ANALYSIS hallazgo 22)
*.npz
analizadas/**/stems/

# Datos de usuario legacy — migrado a projects/<slug>/show.json (ANALYSIS hallazgo 23)
/show_timeline.json

# Autosaves de runtime — rotación automática del backend, NO versionar
**/autosave/

# Render offline (regenerable) y caché de build del frontend
**/render_meta.json
*.tsbuildinfo

# Temporales y salidas sueltas
*.tmp
_test_out.txt

# Previews de render sueltas en la raíz (regenerables por scripts/_render_*.py)
/red_sun_*.png

# Legacy retirado (migración a projects/<slug>/ completada) — no recrear en git
/fixtures.json

# Generado por el backend en CADA arranque (ShowSession.__init__ → sync_rig_layout)
# y tras cada mutación del rig. No versionar: churn puro de runtime.
web/public/v3d/rig_layout.json
web/dist/v3d/rig_layout.json

# Internal dev docs (kept locally, out of the public portfolio)
CLAUDE.md
ANALYSIS.md
SETUP.md
STRUCTURE.md
